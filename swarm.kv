#:kivy 1.10.0

<AgentWidget>:
    canvas:
        Color:
            rgba: 1, 0, 0, 1
        Ellipse:
            size: self.size
            pos: self.pos

<Field>:
    # TODO: use texture with repeat patter instead of this
    agent_widget: agent
    AgentWidget:
        id: agent
        size: 20, 20
